# Git specific files
.git
.gitignore
.gitattributes

# Docker specific files
.dockerignore
Dockerfile* # Exclude the Dockerfile itself from being copied in

# Python specific cache/compiled files
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual Environments (shouldn't be copied anyway, but good practice)
.env/
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Environment variables / Secrets file - VERY IMPORTANT!
.env
.env.*

# IDE / Editor files
.idea/
.vscode/
*.sublime-*

# OS specific files
.DS_Store
Thumbs.db

# Test files and cache (if not needed in production image)
.pytest_cache/
.tox/
.nox/
htmlcov/
.coverage*
# tests/ # Uncomment if your tests are large and not needed in prod image

# Local databases (if not needed in image)
*.sqlite
*.sqlite3

# Other logs or local config
*.log
local_settings.py

# Node.js artifacts if you happen to have them for frontend build steps
# node_modules/
# npm-debug.log*
# yarn-debug.log*
# yarn-error.log*